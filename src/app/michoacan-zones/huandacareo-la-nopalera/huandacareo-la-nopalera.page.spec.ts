import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HuandacareoLaNopaleraPage } from './huandacareo-la-nopalera.page';

describe('HuandacareoLaNopaleraPage', () => {
  let component: HuandacareoLaNopaleraPage;
  let fixture: ComponentFixture<HuandacareoLaNopaleraPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(HuandacareoLaNopaleraPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
