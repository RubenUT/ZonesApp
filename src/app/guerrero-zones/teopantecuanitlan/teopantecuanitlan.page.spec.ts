import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TeopantecuanitlanPage } from './teopantecuanitlan.page';

describe('TeopantecuanitlanPage', () => {
  let component: TeopantecuanitlanPage;
  let fixture: ComponentFixture<TeopantecuanitlanPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(TeopantecuanitlanPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
